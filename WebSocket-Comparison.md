# 키오스크(Kiosk)와 오퍼레이터(Operator) 앱의 WebSocket 비교 분석

## 1. 개요

본 문서는 로보다인 서비스의 키오스크(Kiosk)와 오퍼레이터(Operator) 앱에서 사용되는 WebSocket 연결 방식과 데이터 활용 패턴을 비교 분석합니다. 두 앱은 동일한 백엔드 서비스와 통신하지만, 각자의 역할과 목적에 맞게 WebSocket을 다른 방식으로 구현하고 있습니다.

## 2. 아키텍처 비교

### 2.1 키오스크(Kiosk) 앱

**구조:**
- **WebSocketContext**: 전역 컨텍스트로 WebSocket 연결 관리
- **useWebSocketSingleton**: 싱글톤 패턴으로 앱 전체에서 공유되는 단일 WebSocket 연결 관리
- **useWebSocketManager**: 개별 WebSocket 연결을 관리하는 훅
- **useWebSocket**: 컴포넌트에서 특정 토픽의 WebSocket 데이터에 접근하는 편의성 훅

**주요 토픽:**
- `menu`: 메뉴 데이터 관리
- `orders`: 주문 정보 관리

### 2.2 오퍼레이터(Operator) 앱

**구조:**
- **WebSocketContext**: 전역 컨텍스트로 WebSocket 연결 관리
- 직접 컨텍스트를 통해 데이터에 접근 (`useWebSockets()` 훅 사용)
- 싱글톤 패턴 미적용 (컨텍스트 내부에서 모든 관리)

**주요 토픽:**
- `robots`: 로봇 상태 정보
- `tables`: 테이블 정보
- `events`: 이벤트 데이터
- `orders`: 주문 정보
- `status`: 시스템 상태
- `systemlogs`: 시스템 로그
- `customers`: 고객 정보
- `inventory`: 재고 정보
- `video_streams`: 비디오 스트림
- `notifications`: 알림
- `commands`: 명령어
- `chat`: 채팅 메시지
- `menu`: 메뉴 정보

## 3. 연결 관리 방식 비교

### 3.1 키오스크(Kiosk) 앱

**특징:**
1. **싱글톤 패턴 적용**:
   - `useWebSocketSingleton.js` 통해 전역 WebSocket 인스턴스 공유
   - 모든 컴포넌트가 동일한 연결 인스턴스 재사용

2. **구독 모델 구현**:
   - 토픽별로 구독자(subscribers) 관리
   - 첫 구독자가 생길 때 연결 생성, 마지막 구독자 제거 시 연결 종료
   - 구독/구독해제 패턴으로 효율적인 리소스 관리

3. **지연 연결 해제**:
   - 마지막 구독자 제거 후 500ms 지연 후 연결 종료
   - 빠른 재구독 시 연결을 유지하여 효율성 확보

4. **이벤트 디바운싱**:
   - 연결 상태 이벤트는 100ms 지연 적용
   - 데이터 메시지는 즉시 전달하여 반응성 유지

### 3.2 오퍼레이터(Operator) 앱

**특징:**
1. **컨텍스트 중심 관리**:
   - 모든 토픽 연결이 WebSocketContext 내부에서 직접 관리
   - 앱 시작 시 모든 토픽에 대한 연결을 한 번에 설정

2. **로컬 스토리지 로깅**:
   - 연결 상태와 이벤트를 로컬 스토리지에 기록하여 디버깅 지원
   - 각 토픽별 최근 5개 이벤트 저장

3. **브라우저 알림 통합**:
   - `notifications` 토픽 데이터 수신 시 브라우저 알림(Notification API) 표시
   - PENDING 상태 알림만 필터링하여 표시

4. **HTTP API와 통합**:
   - 헬스체크 기능에서 WebSocket 연결 상태와 API 상태를 함께 확인
   - 서비스 전체 상태 모니터링에 활용

## 4. 데이터 처리 방식 비교

### 4.1 키오스크(Kiosk) 앱

**특징:**
1. **데이터 정규화**:
   - 받은 데이터를 일관된 구조로 정규화
   - `MenuItem.id`/`id` 같은 다양한 형태의 필드 통일

2. **중복 데이터 필터링**:
   - 이전 수신 데이터와 새로운 데이터를 비교하여 중복 처리 방지
   - 효율적인 데이터 비교 알고리즘 구현 (항목 수 기준)

3. **데이터 요청 최적화**:
   - 이미 데이터가 있는 경우 불필요한 요청 방지
   - 요청 플래그 사용하여 중복 요청 관리

4. **캐싱 메커니즘**:
   - 마지막 수신 데이터 캐싱
   - 새 구독자에게 캐시된 데이터 즉시 제공

### 4.2 오퍼레이터(Operator) 앱

**특징:**
1. **데이터 병합 전략**:
   - 기존 데이터와 새로운 데이터를 지능적으로 병합
   - 식별자 기반 맵을 사용하여 효율적인 데이터 업데이트

2. **토픽별 특화 처리**:
   - `orders`: 주문, 주문항목, 메뉴아이템 간의 관계 처리
   - `notifications`: PENDING 상태 필터링
   - `chat`: 메시지 형식에 상관없이 처리 가능한 로직

3. **에러 처리와 데이터 검증**:
   - 데이터 구조 검증 후 일관된 형식으로 변환
   - 데이터가 비어있는 경우 기존 데이터 유지

4. **복잡한 상태 관리**:
   - `OrdersPage`에서 useRef를 활용한 데이터 캐싱
   - useMemo를 통해 파생 데이터 계산 최적화

## 5. 재연결 전략 비교

### 5.1 키오스크(Kiosk) 앱

**특징:**
1. **지수 백오프 재연결**:
   - 초기 재연결 지연 시간: 3초
   - 1.5배씩 증가하는 지수 백오프
   - 최대 재연결 지연 시간: 30초
   - 최대 재연결 시도 횟수: 5회

2. **토픽별 독립적 재연결 관리**:
   - 각 토픽 연결이 독립적으로 재연결 시도
   - 하나의 토픽 연결 문제가 다른 토픽에 영향 없음

3. **디바운스된 새로고침**:
   - 토픽 새로고침 요청에 1.5초 디바운스 적용
   - 짧은 시간 내 중복 새로고침 방지

### 5.2 오퍼레이터(Operator) 앱

**특징:**
1. **유사한 지수 백오프 패턴**:
   - 초기 재연결 지연 시간: 3초
   - 1.5배씩 증가하는 지수 백오프
   - 최대 재연결 지연 시간: 30초
   - 최대 재연결 시도 횟수: 5회

2. **연결 타임아웃 적용**:
   - 연결 시도 5초 후 타임아웃 적용
   - 타임아웃 발생 시 재연결 프로세스 시작

3. **정상/비정상 종료 구분**:
   - 정상 종료(코드 1000, 1001)와 비정상 종료 구분
   - 비정상 종료일 경우에만 자동 재연결 시도

## 6. 에러 처리 비교

### 6.1 키오스크(Kiosk) 앱

**특징:**
1. **구조화된 에러 관리**:
   - 토픽별 에러 상태 관리
   - 에러 발생 시 구독자에게 즉시 알림

2. **세분화된 에러 유형**:
   - 연결 오류
   - 메시지 파싱 오류
   - 최대 재연결 시도 초과 오류

3. **에러 복구 전략**:
   - 에러 발생 후 사용자에게 재시도 옵션 제공
   - API 폴백: WebSocket 연결 실패 시 HTTP API 사용

### 6.2 오퍼레이터(Operator) 앱

**특징:**
1. **로컬 스토리지 에러 로깅**:
   - 에러 정보를 로컬 스토리지에 저장하여 디버깅 지원
   - 최근 에러 이력 추적 가능

2. **헬스체크와 통합된 에러 감지**:
   - WebSocket 연결 상태를 시스템 헬스체크에 포함
   - 연결 문제 발생 시 전체 시스템 상태 변경

3. **에러 메시지 추출 및 정규화**:
   - 다양한 형태의 에러를 일관된 메시지로 변환
   - 에러 객체, 이벤트, 문자열 등 다양한 형태 지원

## 7. 성능 및 효율성 비교

### 7.1 키오스크(Kiosk) 앱

**특징:**
1. **사용 기반 연결 관리**:
   - 필요할 때만 연결 유지 (구독자 기반)
   - 사용하지 않는 연결 자동 정리

2. **메모리 효율성**:
   - 최소한의 데이터만 캐싱
   - ref를 활용한 불필요한 렌더링 방지

3. **데이터 정규화로 일관성 유지**:
   - 다양한 데이터 형식을 일관된 구조로 변환
   - 데이터 처리 로직 단순화

### 7.2 오퍼레이터(Operator) 앱

**특징:**
1. **다중 토픽 항상 연결 유지**:
   - 필요한 모든 토픽에 항상 연결
   - 즉각적인 데이터 수신 보장

2. **복잡한 데이터 처리 최적화**:
   - 대량의 다양한 데이터 효율적 처리
   - 메모이제이션을 통한 계산 최적화

3. **사용자 인터페이스 통합**:
   - 브라우저 알림과의 통합
   - 시스템 상태 모니터링과 통합

## 8. 결론

### 8.1 키오스크(Kiosk) 앱

키오스크 앱은 최종 사용자 기기에서 실행되는 특성을 고려하여 **자원 효율성과 신뢰성**에 중점을 둔 WebSocket 구현을 채택했습니다. 싱글톤 패턴과 구독 모델을 통해 필요한 연결만 유지하고, 세밀한 오류 처리와 재연결 메커니즘으로 안정적인 사용자 경험을 제공합니다. 특히 메뉴와 주문 데이터에 집중하여 필요한 데이터만 효율적으로 처리합니다.

### 8.2 오퍼레이터(Operator) 앱

오퍼레이터 앱은 시스템 관리자와 매장 직원을 위한 도구로서 **포괄적인 데이터 접근과 실시간성**에 초점을 맞춘 WebSocket 구현을 갖추고 있습니다. 다수의 토픽을 항상 연결하여 모든 시스템 영역에 대한 실시간 데이터를 유지하고, 복잡한 데이터 병합 및 관계 처리 로직을 통해 다양한 데이터를 일관되게 표시합니다. 브라우저 알림과 같은 추가 기능을 통합하여 운영자 경험을 향상시킵니다.

### 8.3 공통점

두 앱 모두 다음과 같은 공통된 WebSocket 관리 패턴을 공유합니다:
- 지수 백오프 기반 재연결 전략
- React Context API를 활용한 전역 상태 관리
- 토픽별 데이터 및 연결 상태 추적
- 효율적인 에러 처리 및 복구 메커니즘

이러한 아키텍처 차이는 각 앱의 사용 맥락과 요구사항을 반영하며, 동일한 백엔드 시스템과 효과적으로 통신하면서도 각자의 역할에 최적화된 구현을 보여줍니다. 